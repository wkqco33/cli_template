# 줄바꿈을 LF로 고정한다. Windows 체크아웃에서 CRLF가 섞이면
# `gofmt -l`가 모든 파일을 위반으로 판단한다.
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.gz binary
*.zip binary
*.tar.gz binary
